<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ljb.materialreader">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!--相机-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.CaptureActivity"/>
        <activity android:name=".ui.activity.SearchResultActivity"/>
        <activity android:name=".ui.activity.BookDetailActivity"/>
        <activity android:name=".ui.activity.BookReviewsActivity"/>
        <activity android:name=".ui.activity.WebViewActivity"/>
        <activity android:name=".ui.activity.CategoryDetailActivity"/>
        <activity android:name=".ui.activity.ESearchResultActivity"/>
        <activity android:name=".ui.activity.EBookDetailActivity"/>
        <activity android:name=".ui.activity.EBookReviewsActivity"/>
        <activity android:name=".ui.activity.EBookZoneActivity"/>
    </application>

</manifest>